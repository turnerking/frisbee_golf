require 'spec_helper'

describe Hole do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Hole.create!(@valid_attributes)
  end
end
